export enum RequstedType {
  SEGMENT_SEARCH = 'requested email or paypal search on part of email',
  EMAIL_SEARCH = 'requested full search by email',
  BANK_OR_PAYPAL_SEARCH = 'requested full search on bank account or paypal',
  MULTIPLE = ' requested multiple searh'
}